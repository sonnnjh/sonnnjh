어제 소스 분석
1. Member클래스의 myList배열의 선언이유?
   :각각의 Member가 개별적으로 콕한 물품들을 저장하기 위한 저장공간을 할당함.
    Member 클래스에 선언한 이유는 회원 개별적으로 다 다르기 때문에.
☞ mylist배열 변수는 굿즈객체의 주소를 저장한다. 선언의 목적은 중고물품 객체 리스트 중 콕한 객체의 주소를 저장하기 위해서.

2. Member클래스의 getList() 메서드를
   코드에서 정의한 것을 분석하고
   어떤 목적으로 호출했는지 코드와 함께 분석
   : 정의 - 리턴타입:배열의 주소값, 리턴값:myList배열의 주소값 
   :  호출한 목적 - ..
☞getlist매서드의 리턴타입은 good배열이다. 매개변수는 없다. 리턴값은 mylist라는 배열의 주소갑이다.
   굿즈리스트 매서드를 호출하면 리턴값으로 콕한 리스트 배열의 주소를 리턴한다.
   five2ma에서 콕한 목록을 보고자 할 때, getlist를 호출한다면 콕한 목록이 저장되어있는 배열변수를 리턴받아 
   콕한 배열의 접근이 가능하다.

3. Member 클래스의 myList와
   Five2Ma 클래스의 myList메서드의 myList는
   같은 변수인가? 서로 다른 변수인가?
   이유는 무엇인가?
   : 같은 변수이다. 같은 변수에 다른 값을 넣었다. 이유는 자료형과 변수명이 같기 때문이다.
☞다른변수이다. 변수공부하는 법. 선언했을때 자료형'
  이유:선언된 위치가 다르다 Five2Ma 클래스의 myList메서드의 myList는메서드에 선언된 메서드


4. gList의 1번 을 찜했다고 가정한다.
  만약 gList의 1번의 객체를 삭제 했다면
  찜한 목록에는 어떻게 될까?
  이유는 무엇일까?
  : 삭제가 된다.
  gList는 게시한 중고물품 저장하는 배열이기 때문에
  중고물품을 삭제하면 찜한 목록에도 삭제가 된다.
  찜한 목록은 gList의 주소를 주입받았기 때문이다.
☞glist의 주소를 삭제했다면 찜목록에는 남아있을것


5. gList의 1번 을 찜했다고 가정한다.
  만약 gList의 1번의 객체의 name변수의 값을 수정했다면
  찜한 목록의 객체의 name값도
  변경될까? 변경되지 않을까?
  이유는 무엇일까?
  : 변경 된다.
  : 찜한 목록은 gList의 주소를 주입받았기 때문이다.
☞변경이된다